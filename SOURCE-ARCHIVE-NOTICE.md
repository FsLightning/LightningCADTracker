# Source Archive Notice

GitHub Release 页面会自动显示 `Source code (zip/tar.gz)` 链接，这些链接无法隐藏。

本仓库是 `LightningCAD` 的需求、缺陷、任务与安装包发布中心，不包含 `LightningCAD` 源代码。Release 页面中的 source archive 只是本 Tracker 仓库归档，不是安装包。

安装或测试时请下载 Release Assets 中的 MSI 文件：

```text
LightningCAD_Installer_vX.Y.Z.msi
```
